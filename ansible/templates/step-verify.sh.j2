#!/bin/bash

[ -f /.ok ] && echo done ||
{{ item.1.verify | default('echo') }}  &&
echo done || exit 0

# TASK_SCORE="{{ item.1.score | default(1) }}"
# source /opt/.user
# cat << EOF | curl -s -X POST --data @- https://s9cfrymdt8.execute-api.eu-west-1.amazonaws.com/default/lab_logger >/dev/null 2>&1
# {
#   "table": "{{ module.db_name }}",
#   "timestamp": "$(date --iso-8601=ns --utc)",
#   "payload": {
#     "name": "${FIRSTNAME} ${LASTNAME}",
#     "email": "${EMAIL}",
#     "scenario": "{{ course.course_id }}.{{ item.0 }}", 
#     "score": ${TASK_SCORE}
#   }
# }
# EOF


